/**
 * PencilController
 *
 * @description :: Server-side logic for managing pencils
 * @help        :: See http://links.sailsjs.org/docs/controllers
 */

module.exports = {

  // find()
  // create()
  // update()
  // destroy()

  _config: {
    // Make blueprint routes '/widgets' instead of '/widget'
    pluralize: true
  }
};

